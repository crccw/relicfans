<%= form_for(@museum) do |f| %>
  <% if @museum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@museum.errors.count, "error") %> prohibited this museum from being saved:</h2>

      <ul>
      <% @museum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :home_url %><br>
    <%= f.text_field :home_url %>
  </div>
  <div class="field">
    <%= f.label :exhibit_url %><br>
    <%= f.text_field :exhibit_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
